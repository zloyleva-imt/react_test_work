const initState = {
    products: [],
}

export const products = (state = initState, {type, payload}) => {

    // switch (type) {
    //     case SET_TOKEN:
    //         return {
    //             ...state,
    //             token: payload
    //         }
    //     default:
    //         return state
    // }

    return state
};